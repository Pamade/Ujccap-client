import React from "react";
// import { useFetchOffers } from "../../../../hooks/useFetchOffersWithFilter";

const Featured = () => {
  // const { offers } = useFetchOffers(false);
  // console.log(offers);
  return <div>NewestOffers</div>;
};

export default Featured;
